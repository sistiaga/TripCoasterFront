/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TripResponse } from '../../models/trip-response';

export interface RemoveCountryFromTrip$Params {

/**
 * Trip ID
 */
  id: number;

/**
 * Country ID
 */
  countryId: number;
}

export function removeCountryFromTrip(http: HttpClient, rootUrl: string, params: RemoveCountryFromTrip$Params, context?: HttpContext): Observable<StrictHttpResponse<TripResponse>> {
  const rb = new RequestBuilder(rootUrl, removeCountryFromTrip.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('countryId', params.countryId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TripResponse>;
    })
  );
}

removeCountryFromTrip.PATH = '/trips/{id}/countries/{countryId}';
