/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PoisListResponse } from '../../models/pois-list-response';

export interface GetPoisByLocationId$Params {

/**
 * Location ID
 */
  locationId: number;
}

export function getPoisByLocationId(http: HttpClient, rootUrl: string, params: GetPoisByLocationId$Params, context?: HttpContext): Observable<StrictHttpResponse<PoisListResponse>> {
  const rb = new RequestBuilder(rootUrl, getPoisByLocationId.PATH, 'get');
  if (params) {
    rb.path('locationId', params.locationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PoisListResponse>;
    })
  );
}

getPoisByLocationId.PATH = '/pois/location/{locationId}';
